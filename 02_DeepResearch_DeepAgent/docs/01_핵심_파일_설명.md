# 핵심 파일별 역할 설명

## 📁 파일 구조 개요

```
src/
├── configuration.py          # ⚙️ 설정 관리
├── state.py                  # 📊 상태 정의
├── separate_agent.py         # 🤖 메인 에이전트
├── utils.py                  # 🛠️ 유틸리티
├── prompts/                  # 💬 프롬프트
│   ├── orchestrator.py       # 오케스트레이터 프롬프트
│   ├── researcher.py         # 연구자 프롬프트
│   ├── compressor.py         # 압축기 프롬프트
│   ├── critic.py             # 비평가 프롬프트
│   └── webpage_summarize.py  # 웹페이지 요약 프롬프트
├── subagents/                # 🎯 서브에이전트
│   ├── researcher.py         # 연구자 에이전트
│   ├── compressor.py         # 압축 에이전트
│   ├── critic.py             # 비평 에이전트
│   └── dynamic.py            # 동적 에이전트
├── skills/                   # 🎨 스킬 관리
│   ├── registry.py           # 스킬 레지스트리
│   └── tool_collections.py   # 도구 컬렉션
└── tools/                    # 🔧 커스텀 도구
    └── subagent_tools.py     # 서브에이전트 도구
```

---

## 1️⃣ configuration.py - 설정 관리

### 역할
시스템의 모든 설정을 중앙에서 관리하는 파일입니다.

### 주요 클래스

#### `SearchAPI` (Enum)
```python
class SearchAPI(str, Enum):
    ANTHROPIC = "anthropic"
    OPENAI = "openai"
    TAVILY = "tavily"
    NONE = "none"
```
- **역할**: 사용 가능한 검색 API 제공자 정의
- **사용**: 어떤 검색 엔진을 사용할지 선택

#### `MCPConfig` (BaseModel)
```python
class MCPConfig(BaseModel):
    url: Optional[str] = None
    tools: list[str] = Field(default_factory=list)
    auth_required: bool = False
```
- **역할**: MCP (Model Context Protocol) 서버 설정
- **사용**: 외부 도구 서버 연결 시 필요

#### `DeepAgentConfiguration` (BaseModel)
```python
class DeepAgentConfiguration(BaseModel):
    # 모델 설정
    main_model: str = "openai:gpt-4.1"
    researcher_model: Optional[str] = None
    summarization_model: str = "openai:gpt-4.1"
    
    # 검색 설정
    search_api: SearchAPI = SearchAPI.TAVILY
    max_content_length: int = 50000
    
    # DeepAgent 전용
    max_parallel_researchers: int = 5
    max_researcher_iterations: int = 10
    enable_critique_phase: bool = False
```

### 주요 설정 항목 설명

| 설정 | 기본값 | 설명 |
|------|--------|------|
| `main_model` | `"openai:gpt-4.1"` | 메인 오케스트레이터가 사용할 LLM 모델 |
| `max_parallel_researchers` | `5` | 동시에 실행할 수 있는 연구자 수 |
| `max_researcher_iterations` | `10` | 연구 반복 최대 횟수 |
| `enable_critique_phase` | `False` | 비평가 에이전트 활성화 여부 |
| `compression_threshold_tokens` | `150000` | 압축을 시작하는 토큰 임계값 |

### 사용 예시
```python
# 설정 생성
config = DeepAgentConfiguration(
    main_model="anthropic:claude-sonnet-4",
    max_parallel_researchers=3,
    enable_critique_phase=True
)

# 런타임 설정으로 변환
runnable_config = config.to_runnable_config()
```

---

## 2️⃣ state.py - 상태 정의

### 역할
시스템의 모든 상태 구조를 정의합니다. LangGraph는 상태 기반으로 동작하므로 매우 중요한 파일입니다.

### 주요 클래스

#### 1. 구조화된 출력 모델

##### `ConductResearch`
```python
class ConductResearch(BaseModel):
    research_topic: str  # 연구할 주제
```
- **역할**: 새로운 연구 작업을 시작할 때 사용
- **예시**: "AI 윤리에 대한 최신 논의"

##### `ResearchComplete`
```python
class ResearchComplete(BaseModel):
    pass  # 완료 신호만 필요
```
- **역할**: 연구가 완료되었음을 알림
- **사용**: 연구자가 작업을 마쳤을 때 호출

##### `Summary`
```python
class Summary(BaseModel):
    summary: str          # 요약 내용
    key_excerpts: str     # 핵심 발췌
```
- **역할**: 웹페이지나 문서 요약 결과 저장

#### 2. 상태 정의

##### `AgentState` (메인 상태)
```python
class AgentState(MessagesState):
    supervisor_messages: list[MessageLikeRepresentation]  # 감독자 메시지
    research_brief: str | None                            # 연구 개요
    raw_notes: list[str]                                  # 원시 노트
    notes: list[str]                                      # 정리된 노트
    final_report: str                                     # 최종 보고서
    compressed_research_length: int = 0                   # 압축된 연구 길이
    raw_notes_length: int = 0                             # 원시 노트 길이
```

**각 필드 설명:**
- `supervisor_messages`: 오케스트레이터와 서브에이전트 간 통신 메시지
- `research_brief`: 사용자 질문을 기반으로 생성된 연구 계획
- `raw_notes`: 연구자들이 수집한 가공되지 않은 정보
- `notes`: 압축기가 정리한 구조화된 정보
- `final_report`: 최종 생성된 보고서

##### `SupervisorState` (감독자 상태)
```python
class SupervisorState(TypedDict):
    supervisor_messages: list[MessageLikeRepresentation]
    research_brief: str
    notes: list[str]
    research_iterations: int = 0  # 연구 반복 횟수
    raw_notes: list[str]
```
- **역할**: 연구 진행 상황을 추적하고 관리

##### `ResearcherState` (연구자 상태)
```python
class ResearcherState(TypedDict):
    researcher_messages: list[MessageLikeRepresentation]
    tool_call_iterations: int = 0      # 도구 호출 횟수
    research_topic: str                # 연구 주제
    compressed_research: str           # 압축된 연구 결과
    raw_notes: list[str]               # 원시 노트
```
- **역할**: 개별 연구자의 작업 상태 추적

### 리듀서 함수

#### `override_reducer`
```python
def override_reducer(current_value, new_value):
    if isinstance(new_value, dict) and new_value.get("type") == "override":
        return new_value.get("value", new_value)
    else:
        return operator.add(current_value, new_value)
```
- **역할**: 상태 업데이트 방식 제어
- **동작**: 
  - 기본: 리스트에 추가 (append)
  - override 타입: 완전히 대체

---

## 3️⃣ separate_agent.py - 메인 에이전트

### 역할
전체 시스템의 진입점이자 메인 에이전트를 생성하는 파일입니다.

### 주요 함수

#### `create_deep_research_agent`
```python
async def create_deep_research_agent(
    tools: list[Any],
    model: str = "openai:gpt-4.1",
    max_researcher_iterations: int = 10,
    enable_critique: bool = False,
    enable_clarification: bool = True,
    mcp_prompt: str = "",
    config: Optional[RunnableConfig] = None,
    checkpointer=None,
) -> CompiledStateGraph:
```

**매개변수 설명:**
- `tools`: 사용할 도구 리스트 (tavily_search 등)
- `model`: 사용할 LLM 모델
- `max_researcher_iterations`: 최대 연구 반복 횟수
- `enable_critique`: 비평가 활성화 여부
- `enable_clarification`: 명확화 단계 활성화 여부
- `checkpointer`: 상태 영속화를 위한 체크포인터

**동작 과정:**
1. 도구를 글로벌 레지스트리에 등록
2. 서브에이전트 설정 생성 (Researcher, Compressor, Critic)
3. 오케스트레이터 프롬프트 포맷팅
4. DeepAgent 생성 및 반환

#### `run_research`
```python
async def run_research(
    question: str,
    tools: list[Any],
    model: str = "anthropic:claude-sonnet-4-5-20250929",
    thread_id: str = "default",
    max_researcher_iterations: int = 10,
    enable_critique: bool = False,
    enable_clarification: bool = True,
    checkpointer=None,
) -> dict[str, Any]:
```

**역할**: 연구 질문을 받아 전체 연구 프로세스를 실행

**사용 예시:**
```python
result = await run_research(
    question="AI의 최신 동향은?",
    tools=[tavily_search, think_tool],
    model="openai:gpt-4",
    thread_id="session_123"
)
```

### 내부 구조

```python
# 1. 서브에이전트 생성
researcher_config = create_researcher_subagent(tools, date, mcp_prompt)
compressor_config = create_compressor_subagent(date)

# 2. 서브에이전트 리스트 구성
subagents = [researcher_config, compressor_config]
if enable_critique:
    subagents.append(create_critic_subagent(date))

# 3. DeepAgent 생성
agent = create_deep_agent(
    model=model,
    tools=tools,
    system_prompt=orchestrator_prompt,
    subagents=subagents,
    backend=FilesystemBackend(...),
    checkpointer=checkpointer,
)
```

---

## 4️⃣ utils.py - 유틸리티 함수

### 역할
시스템 전반에서 사용되는 공통 유틸리티 함수들을 제공합니다.

### 주요 함수 (추정)

#### `get_agent_workspace`
```python
def get_agent_workspace(agent_name: str) -> str:
    """에이전트별 작업 공간 경로 반환"""
```
- **역할**: 각 에이전트의 파일 저장 경로 제공
- **예시**: `workspace/main_agent/`, `workspace/researcher_1/`

#### 날짜/시간 관련
```python
def get_today_str() -> str:
    """현재 날짜를 문자열로 반환"""
    # 예: "Mon Jan 15, 2024"
```

#### 메시지 처리
```python
def get_buffer_string(messages: list) -> str:
    """메시지 리스트를 문자열로 변환"""
```

---

## 5️⃣ prompts/ - 프롬프트 템플릿

### orchestrator.py
```python
def format_orchestrator_prompt(
    date: str,
    max_researcher_iterations: int,
    enable_clarification: bool,
    mcp_prompt: str
) -> str:
```
- **역할**: 메인 오케스트레이터의 시스템 프롬프트 생성
- **내용**: 
  - 연구 프로세스 단계 설명
  - 서브에이전트 사용 방법
  - 파일시스템 사용 규칙

### researcher.py
```python
RESEARCHER_SYSTEM_PROMPT = """
당신은 특정 주제에 대한 심도 있는 연구를 수행하는 전문 연구자입니다.
- tavily_search를 사용하여 정보 수집
- 발견한 내용을 /output/notes/{topic}.md에 저장
- 완료 시 ResearchComplete 호출
"""
```

### compressor.py
```python
COMPRESSOR_SYSTEM_PROMPT = """
당신은 여러 연구 노트를 읽고 종합하는 전문가입니다.
- /output/notes/의 모든 파일 읽기
- 중복 제거 및 정보 구조화
- /output/compressed_research.md에 결과 저장
"""
```

### critic.py
```python
CRITIC_SYSTEM_PROMPT = """
당신은 연구 보고서의 품질을 검증하는 비평가입니다.
- /final_report.md 읽기
- 완전성, 정확성, 구조 평가
- /feedback.md에 피드백 작성
"""
```

---

## 6️⃣ subagents/ - 서브에이전트 정의

### researcher.py
```python
def create_researcher_subagent(
    tools: list[Any],
    date: str,
    mcp_prompt: str = "",
) -> SubAgent:
    return SubAgent(
        name="researcher",
        description="특정 주제에 대한 집중적 연구 수행",
        system_prompt=RESEARCHER_SYSTEM_PROMPT,
        tools=researcher_tools,  # tavily_search, think_tool, ResearchComplete
    )
```

### compressor.py
```python
def create_compressor_subagent(date: str) -> SubAgent:
    return SubAgent(
        name="compressor",
        description="연구 노트 종합 및 압축",
        system_prompt=COMPRESSOR_SYSTEM_PROMPT,
        tools=[],  # 파일시스템 도구만 사용
    )
```

### critic.py
```python
def create_critic_subagent(date: str) -> SubAgent:
    return SubAgent(
        name="critic",
        description="보고서 품질 검증",
        system_prompt=CRITIC_SYSTEM_PROMPT,
        tools=[],
    )
```

### dynamic.py
- **역할**: 런타임에 동적으로 생성되는 서브에이전트
- **사용**: 특수한 작업이 필요할 때 즉석에서 생성

---

## 7️⃣ skills/ - 스킬 관리

### registry.py
```python
class SkillRegistry:
    def __init__(self):
        self._skills: dict[str, list[str]] = {}
        self._tools: dict[str, Any] = {}
        
    def register_tool(self, tool: Any):
        """도구 등록"""
        
    def get_tools_for_skill(self, skill_name: str) -> list[Any]:
        """스킬에 필요한 도구 반환"""
```

**기본 스킬:**
- `web_research`: tavily_search
- `data_analysis`: python_repl
- `writing`: LLM만 사용
- `critique`: LLM만 사용

---

## 8️⃣ tools/ - 커스텀 도구

### subagent_tools.py
```python
class SpawnSubAgent:
    """동적으로 새로운 서브에이전트를 생성하는 도구"""
    
    def __call__(self, task_description: str, required_skills: list[str]):
        # 새로운 서브에이전트 생성 로직
```

---

## 📊 파일 간 관계도

```
separate_agent.py (메인)
    ↓ 사용
configuration.py (설정)
    ↓ 사용
state.py (상태 정의)
    ↓ 사용
subagents/ (서브에이전트)
    ↓ 사용
prompts/ (프롬프트)
    ↓ 사용
skills/registry.py (도구 관리)
    ↓ 사용
tools/ (커스텀 도구)
```

## 🎯 다음 단계

각 파일의 역할을 이해했다면, 이제 **02_상태_관리.md**에서 상태가 어떻게 흐르는지 자세히 알아보세요.
